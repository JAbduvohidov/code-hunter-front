import { Typography } from '@material-ui/core';

const Logo = () => (
  <Typography color={'#f5f5f5'} fontSize={25} fontFamily={'monospace'} fontWeight={'bold'}>{'{codehunter}'}</Typography>
);

export default Logo;
